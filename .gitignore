# Terraform - ignore local state, caches, sensitive files, and editor cruft

# Local Terraform directories
.terraform/

# Terraform state files and backups
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.backup

# Terraform plan output
*.tfplan

# Crash logs
crash.log
crash.*

# Sensitive variable files (may contain secrets)
*.tfvars
*.tfvars.json
*.auto.tfvars
*.auto.tfvars.json

# Override files (local overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.*

# CLI config / credentials
.terraformrc
terraform.rc
.terraform.d/

# Editor and OS files
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Note: Keep .terraform.lock.hcl committed for reproducible provider versions unless you have a specific reason not to.